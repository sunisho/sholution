# Site settings
baseurl = "https://www.sholution.com/"
languageCode = "en-us"
title = "Sholution Consulting"
theme = "hugo-agency-theme"

# Enter your tracking code to enable Google Analytics
googleAnalytics = ""

[params]
  # You can use Markdown syntax for urls [text](//url.to/source) or
  # most longer texts.
  #
  # All icons using Fontawesome's icon font. Look at fortawesome.github.io/Font-Awesome/icons/
  # for more icons. The icons are represented by their corresponding
  # CSS class.

  # To provide some metadata for search engines feel free to add a few
  # information about you and your website.
  name = "Sholution LLC"
  description = "Consulting Services - Strategic Growth and Product Management"

  # Link custom assets relative to /static
  custom_css = []
  custom_js  = []
  favicon    = "favicon.ico"

  # Possibility to disable external/additional fonts
  # Exception: Fontawesome
  disableFonts = false

  # 404 error customization
  [params.error404]
    heading = "Page couldn't be found"
    text = "Please visit [this page](/)"

  # Optional piwik tracking
  #[params.analytics.piwik]
  #  URL = "https://stats.example.com"
  #  ID = "42"
  #  # Track all subdomains with "*.example.com"
  #  domain = "www.example.com"
  #  # Optional integrity check hash
  #  hash = "sha512-RfeD0pacGTqy9m7U6PgehQfS4cc7SIt+e+P+H5e848kEvB/RW84CUGO3O4O3LNbxjevym6KPUZ8muPsMrI8WIw=="

  # Navigation
  [params.navigation]
    brand = "Sholution Consulting"
    # Optional logo as brand stored in img/
    #logo = "logo.svg"
    services = "Services"
    portfolio = "Portfolio"
    about = "About"
    team = "Meet Sunish"
    contact = "Contact"

    # Add custom links. Those can be pre- and postpended to the default menu above.
    # Use the weight attribute to order your custom links. Uncomment the example
    # below and replace the values with your own.

    # prepended links
    #[[menu.prepend]]
    #  url = "http://gohugo.io"
    #  name = "Hugo"
    #  weight = 10

    # postpended links
    #[[menu.postpend]]
  #    url = "http://gohugo.io"
  #    name = "Hugo"
  #    weight = 10


  # Hero section
  [params.hero]
    # To change the background image of the hero, replace 'header-bg.jpg' in
    # the 'static/img' folder.
    title = "Helping companies"
    subtitle = "Grow and make great products"
    buttonText = "Tell me more"


  # Services section
  [params.services]
    enable = true
    title = "Services"
    subtitle = "I have expertise in various areas that can help you grow and make great products. <BR>If you don't see your need listed below, reach out - if I can't help you, I may know someone who can."

    [[params.services.row]]

    [[params.services.row.list]]
      icon = "fa-external-link-square"
      title = "Growth"
      description = "Business Development, <BR>Partnerships, <BR>Expansion, <BR>Sales Strategy, <BR>Pitch Decks"
      url = "#"

    [[params.services.row.list]]
      icon = "fa-cogs"
      title = "Product"
      description = "Roadmapping, <BR>Innovation, <BR>Technology Architecture, <BR>Vendor Landscaping, <BR>UX/UI, <BR>QA, <BR>Performance Tracking, <BR>Benchmarking"
      url = "#"

  # Portfolio section
  [params.portfolio]
    enable = true
    # All projects defined in their own files. You can find example projects
    # at 'exampleSite/data/projects'. Copy the 'projects' folder into the 'data' directory
    # at the root of this Hugo site.
    # For more informtion take a look at the README.
    title = "Portfolio"
    subtitle = "Past projects, both as a consultant and as part of my former jobs."

    [params.portfolio.modal]
      client = "Client"
      date = "Year"
      category = "Category"
      buttonText = "Close project"


    # About section
  [params.about]
    enable = false
    title = "About"
    subtitle = "Lorem ipsum dolor sit amet consectetur."
    endpoint = "Be part<br>of our<br>story!"

    # The events will be listed from the top to the bottom.
    # All images are stored under 'static/img/about'.
    # Replace them with your own.
    [[params.about.events]]
      img = "1.jpg"
      date = "2013-2016"
      title = "Leading Innovation & Strategy for the World's Biggest Beer Brands"
      description = "Blah blah beer garage blah blah"

  # Team section
  [params.team]
    enable = true
    # Possibility to center items
    #center = true
    title = "Meet Sunish"
    subtitle = "Harvard MBA, Former Electrical & Computer Engineer"
    description = ""

    # All team member's pictures are stored under 'static/img/team'.
    # Replace them with your own too.
    # Suggestion: Image size should be at least 360px*360px or the alignment might be affected.
    [[params.team.members]]
      img = "sunish.jpg"
      name = "Sunish Oturkar"
      position = "Lead Consultant "
      livesIn = "Boston, MA"
      # For bio markdown and even multiline strings are available.
      bio = "Sunish H. Oturkar is the founder & lead consultant of Sholution. <BR>His sweet spot is at the intersection of technology and people. <BR><BR>He has led marketing & sales innovations for brands like Budweiser and Bud Light, helped refine part of Dell's Enterprise business, managed large infrastructure projects for the Department of Defense, and has even developed firmware for the iPod. <BR><BR>Sunish earned a Bachelors of Science from Northeastern University in Electrical and Computer Engineering and has an MBA from Harvard Business School."
      social = [
        ["fa-linkedin", "https://www.linkedin.com/in/sunisho"]
      ]

  # Client section
    [params.clients_list]
      enable = false
      # Possibility to center items
      #center = true
      # Optional client title specifying the involvement
      #title = "Used and trusted by"

      # All the clients' logos are stored under 'static/img/logos'.
      # Replace them with your clients's logos.
      # Suggestion: .svg in the size of 280px*80px looks good
      [[params.clients]]
        logo = "envato.jpg"
        link = "#"

      [[params.clients]]
        logo = "designmodo.jpg"
        link = "#"

      [[params.clients]]
        logo = "themeforest.jpg"
        link = "#"

      [[params.clients]]
        logo = "creative-market.jpg"
        link = "#"

  # Contact section
  [params.contact]
    enable = true
    title = "Reach Out Now"
    subtitle  = ""
    buttonText = "Send message"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below
    # - upload the generated site to your server
    # - send a dummy email yourself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!

    # It is possible to substitute formspring with other providers
    # as an example the mailout plugin from caddy.
    # Provide the post URL for the form and depending on the provider
    # add some custom JS script to finish the implementation.
    # postURL = "https://example.com/mailout/"

    email = "sunish@sholution.com"

    # Success and error message overwrite for async contact form
    thanks = "Thank you for contacting us."
    #error = "Message could not be sent. Please contact Sunish directly via LinkedIn."

    # 'warning' defines error messages for invalid inputs
    [params.contact.form.name]
      text = "Your Name *"
      warning = "Please enter your name."

    [params.contact.form.email]
      text = "Your Email *"
      warning = "Please enter your email address."

    [params.contact.form.phone]
      text = "Your Phone (All numbers no dashes or spaces) *"
      warning = "Please enter your phone number - all numbers no dashes or spaces"

    [params.contact.form.message]
      text = "Your Message *"
      warning = "Please enter a message."

      # Add optional form fields to identify contact forms
      #[[params.contact.form.hidden]]
      #  name = "someID"
      #  value = "example.com"
      #
      #[[params.contact.form.hidden]]
      # Special values for name such as "page" and "site" will be autofilled
      #  name = "page"

  # Footer section
  [params.footer]
    enable = true
    copyright = "Published under the Apache License 2.0"
